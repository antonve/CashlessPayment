<UserControl x:Class="nmct.ba.cashlessproject.ui.View.ManagementProducts"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="300" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <ListBox Grid.Column="0" x:Name="lstProducts"/>
        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="45" />
                <RowDefinition Height="*" />
                <RowDefinition Height="45" />
            </Grid.RowDefinitions>

            <Grid Grid.Row="0" x:Name="grdControls">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <Button Grid.Column="0" x:Name="btnNew">New</Button>
                <Button Grid.Column="1" x:Name="btnEdit">Edit</Button>
                <Button Grid.Column="2" x:Name="btnDelete">Delete</Button>
            </Grid>

            <StackPanel Grid.Row="1" x:Name="stpForm">
                <TextBlock>Product name</TextBlock>
                <TextBox x:Name="txbName"></TextBox>
                <TextBlock>Price</TextBlock>
                <TextBox x:Name="txbPrice"></TextBox>
            </StackPanel>

            <Grid Grid.Row="2" x:Name="grdFormControls">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <Button Grid.Column="0" x:Name="btnSave">Save</Button>
                <Button Grid.Column="1" x:Name="btnCancel">Cancel</Button>
            </Grid>
        </Grid>
    </Grid>
</UserControl>
